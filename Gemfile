source "https://rubygems.org"

# GitHub Pages-compatible Jekyll stack. Run locally with:
#   bundle install
#   bundle exec jekyll serve
gem "github-pages", group: :jekyll_plugins
gem "webrick"

group :jekyll_plugins do
  gem "jekyll-seo-tag"
  gem "jekyll-sitemap"
end
